# AI Usage Log

This log records material AI assistance used during development. The engineer reviews, edits, tests, and approves all changes.

| Date | Task | AI contribution | Engineer review / outcome |
| --- | --- | --- | --- |
| 2026-08-06 | Initial project analysis | Assisted in converting the assignment into a scoped requirements document and incremental execution plan. | Reviewed assumptions and edited the scope to defer implementation choices until the architecture and scaffold commit. Approved as project documentation. |
| 2026-08-06 | Architecture design | Drafted the modular-monolith architecture, integrity boundaries, package/layer design, hash/verification flows, and Scenario B extension options from the assignment requirements. | Architecture was retained as a design baseline. Global chain sequencing and encryption/redaction trade-offs require validation during implementation. |
| 2026-08-06 | Database schema | Generated the PostgreSQL schema reference and explained table, column, constraint, index, retention, redaction, checkpoint, and export decisions. | Schema was committed as a reviewed design artifact. Runtime migration execution remains pending a local PostgreSQL environment. |
| 2026-08-06 | Spring Boot bootstrap | Generated Maven dependencies, root application class, externalized configuration, and production-oriented defaults. | Bootstrap was committed. The local machine does not yet have Java or Maven, so compilation was not performed. |
| 2026-08-06 | Persistence model correction | Generated Flyway migration, immutable JPA entities, and repositories. Identified that a `GENERATED ALWAYS` identity could not be reserved before hashing; revised it to `GENERATED BY DEFAULT` and removed an invalid contiguous-sequence assumption because PostgreSQL sequences can gap after rollbacks. | Correction is documented in the schema and architecture. It is pending compilation and integration-test review. |
| 2026-08-06 | Integrity and API foundation | Generated canonical JSON/SHA-256 utilities, configurable genesis-hash and payload-encryption properties, DTO validation, response envelopes, and global exception handling. | Static diff checks passed. Canonicalization behaviour and configuration binding require unit tests and runtime review. |
| 2026-08-06 | Scenario A application services | Generated transactional append, AES-GCM payload protection, filtered keyset pagination, and full-chain verification with specific tamper classifications. | Design intentionally avoids logging payload contents. Behaviour is pending Testcontainers integration tests and manual tampering validation. |
| 2026-08-06 | REST and OpenAPI adapter | Generated the three required Scenario A routes, correlation-ID request logging, validation mapping, SpringDoc configuration, and request/response examples. | No update/delete route was generated. Swagger UI must be verified locally after Java, Maven, PostgreSQL, and required environment variables are installed. |
| 2026-08-06 | Java 21 Maven build validation | Ran Maven using Microsoft OpenJDK 21.0.12. The first compile exposed an incorrect Swagger `Parameter` annotation import; corrected the import and reran the build. | `mvn clean verify` succeeded with `javac --release 21`, compiling 41 source files. There are currently no test sources; Swagger UI runtime validation remains pending database and secret configuration. |
| 2026-08-06 | Maven dependency-recovery configuration | Added repository-level Maven arguments to force refreshes and keep dependency resolution in an ignored project-local cache after IntelliJ cached a SpringDoc download timeout. | Re-ran `mvn clean verify` with Microsoft OpenJDK 21.0.12; dependency resolution and compilation succeeded. No tests exist yet, so test execution reported no tests to run. |
| 2026-08-06 | H2 database conversion | Replaced the active PostgreSQL runtime configuration, driver integration, and PostgreSQL-specific migration syntax with embedded H2 2.x configuration and compatible sequence, UUID, binary, and timestamp definitions. Corrected H2's required UUID default/primary-key declaration order in all affected tables. Updated active architecture, schema, and setup documentation accordingly. | `mvn clean verify` succeeded with Microsoft OpenJDK 21.0.12. The complete Flyway SQL script was executed successfully against H2 2.3.232 using `org.h2.tools.RunScript`. H2 is configured for local development only; full application startup and Swagger checks remain manual validation items. |
| 2026-08-06 | Payload-encryption configuration validation | Added startup-time validation that the externally supplied payload key is valid Base64 and decodes to exactly 32 bytes for AES-256. | This prevents an invalid key from being discovered only when the first audit event is appended. The key value is not stored or logged. |
| 2026-08-06 | Scenario A handoff documentation | Updated the README to reflect the implemented Scenario A routes, secure local key setup, H2 console, Swagger UI, health endpoint, and remaining validation scope. | Documentation intentionally does not include an encryption key. Scenario B remains deferred until Scenario A chain behaviour is validated. |
| 2026-08-06 | Scenario B retention | Added configurable logical archival using immutable archive manifests, a serializable retention operation, and an archive endpoint. The operation archives only the next unarchived contiguous prefix, preventing overlapping manifests on retries. | Logical archiving retains source rows in the prototype, so Scenario A verification remains unchanged and valid. Physical movement to immutable object storage is intentionally deferred. |
| 2026-08-06 | Scenario B redaction and export | Added immutable JSON Pointer redaction evidence, response-time redacted projections, a redaction audit event, and actor/resource export bundles containing chain metadata and canonical bundle digests. | Projection redaction preserves evidence but is not per-field cryptographic erasure. Export supports independent hash-input and bundle-digest checks; non-contiguous exports need signed checkpoints or intervening records for a complete global-chain proof. |
| 2026-08-06 | Scenario C compliance reporting | Clarified the under-specified regulator-access request, documented assumptions and boundaries, and added a read-only client-account access report with sequence and hash-chain metadata while omitting plaintext payloads. | The endpoint is a prototype reporting view, not a claim of regulatory compliance. Authentication, regulator delivery, signed checkpoints, legal hold, and jurisdiction-specific policy remain deferred. |
| 2026-08-06 | Swagger UI completion | Extended the existing SpringDoc API group to include compliance reporting and enriched generated API metadata and controller tags. | Swagger UI remains available at `/swagger-ui.html`; runtime loading must be checked after resolving the local Maven/IDE file lock. |
| 2026-08-06 | Redaction compatibility correction | Removed an unavailable Jackson `JsonPointer.mayMatch()` call from the redaction projector. | The projector now uses supported pointer operations and preserves the same response-time redaction behaviour. |
| 2026-08-06 | Persistence-conflict diagnostics | Added safe server-side exception logging for persistence conflicts while retaining the generic API error response. | This exposes the database constraint name in local logs without returning storage internals or payload data to API clients. |
| 2026-08-06 | Assigned-UUID persistence correction | Diagnosed a Hibernate `NonUniqueObjectException` caused by reusing a detached audit event after Spring Data merged its assigned UUID. The append path now uses the managed instance returned by `saveAndFlush` for payload persistence and the chain head. | Fix is based on the captured local stack trace. It prevents duplicate managed instances for one audit event identifier. |
| 2026-08-06 | Phase 8 validation review | Audited repository status, API surface, schema/migrations, Swagger configuration, and test sources; added a testing plan and ran Maven validation. | After releasing Windows locks, `mvn clean test` succeeded under Java 21.0.12. The suite ran 2 integration tests with 2 passed, 0 failed, 0 errors, and 0 skipped. |
| 2026-08-06 | Automated integration-test foundation | Added an H2/Spring Boot MockMvc integration suite for core Scenario A/B/C flows and invalid-input handling. Corrected an initial JSON-fixture syntax error discovered by Maven. | The finalized `mvn clean test` run passed: 2 tests, 0 failures, 0 errors, and 0 skipped. |
| 2026-08-06 | Timestamp hash-stability correction | Automated verification exposed a content-hash mismatch caused by hashing nanosecond timestamps while H2 persisted microseconds. The append service now truncates server timestamps to microseconds before hashing and persistence. | This is a correctness fix required for stable hash-chain verification with the configured H2 schema. |
| 2026-08-07 | Swagger UI usability | Added Swagger UI sorting, Try-it-out, request-duration, and no-persisted-authorisation settings. Documented correct header/body entry in Swagger UI. | No authentication scheme exists in the prototype, so authorization persistence is disabled. |
| 2026-08-07 | Swagger grouped-document correction | Diagnosed the duplicated group name in Swagger UI's OpenAPI URL and removed the conflicting explicit URL configuration. Added normal 404 handling for missing static resources. | SpringDoc grouped API discovery now owns the group document URL, preventing the Swagger fetch 500. |
| 2026-08-07 | Live Scenario A-C validation | Exercised the running local service with fictional HTTP requests covering append, filtered pagination, verification, redaction, duplicate-redaction conflict, export selector guards, compliance reporting, invalid date range, and retention no-op behaviour. | All exercised HTTP status expectations passed. Retention returned `204` under the default one-year policy and verification remained intact; the eligible-archive `201` path requires a separate `PT0S` configuration run. |
| 2026-08-07 | Submission attestation | Applied engineer-supplied identity and submission-date details to the required individual-work attestation. | The attestation now contains no placeholder values; the engineer remains responsible for its accuracy. |

## Operating rules

- Do not include credentials, proprietary customer data, or non-public production information in prompts.
- Treat AI output as a draft, not an authority; verify it through design review, tests, and local execution.
- Record significant generated, modified, or rejected suggestions with the rationale.
- The engineer retains responsibility for correctness, security, maintainability, and release decisions.
- Before each component commit, update this log with the material AI contribution, engineer decision, validation performed, and any known limitation.
- Before committing, validate the log with `git diff --check` and confirm that the entry does not contain credentials, payload data, or unsupported claims.
- After a commit and push, record the resulting commit reference and push outcome in the relevant entry or the next log entry.
| 2026-08-12 | Item 1 architecture reconciliation | Replaced aspirational architecture claims with the implemented `com.auditlog` layered design; added ArchUnit boundary checks and an application result for retention responses. | Engineer approved the pragmatic layered approach. `mvn clean verify` passed with 6 tests, 0 failures, 0 errors, and 0 skipped; application-to-persistence coupling remains intentionally documented. |
| 2026-08-12 | Item 2 tamper-detection automation | Generated nine H2/MockMvc integration tests that directly tamper with persisted audit data and assert every chain-verification violation plus payload non-disclosure. | Initial verification exposed shared H2 test-data leakage into the existing order-dependent scenario test; an approved `@AfterEach` cleanup was added. Final `mvn clean verify` passed with 15 tests, 0 failures, 0 errors, and 0 skipped. |
| 2026-08-12 | Item 3 integrity-component tests | Generated focused unit tests for canonical JSON, cursors, hash-input ambiguity, AES-GCM, and payload redaction. | Tests exposed null cursor handling as an unhandled `NullPointerException`; an approved guard now returns the documented invalid-cursor error. ArchUnit was restricted to production classes and canonical-output assertions were aligned to deterministic bytes. Final `mvn clean verify` passed with 29 tests, 0 failures, 0 errors, and 0 skipped. |
| 2026-08-12 | Item 4 scenario-test isolation | Replaced the Scenario A-C mega-test with independent behaviour-named tests, configuration-derived genesis assertion, JSON parsing, and H2 setup/cleanup. | Production code was unchanged. Final `mvn clean verify` passed with 35 tests, 0 failures, 0 errors, and 0 skipped. |
| 2026-08-12 | Item 5 concurrent append validation | Generated a service-level eight-thread concurrent append integration test using a coordinated executor and the existing pessimistic chain-head lock. | The test proved sequences 1-8, ordered predecessor links, and intact verification under H2 locking semantics. Final `mvn clean verify` passed with 36 tests, 0 failures, 0 errors, and 0 skipped; PostgreSQL concurrency validation remains unimplemented. |
| 2026-08-12 | Item 6 bounded verification | Added optional sequence-bounded verification with predecessor continuity, streaming fetch-size hints, periodic persistence-context clearing, and response metadata distinguishing complete from bounded verification. | Checkpoint resume was deliberately deferred because checkpoint signatures and trust validation are not implemented. Final `mvn clean verify` passed with 40 tests, 0 failures, 0 errors, and 0 skipped. |
| 2026-08-12 | Item 7 environment independence | Added Maven Wrapper, JaCoCo reporting, GitHub Actions verification, Java 21 Docker packaging, and an optional PostgreSQL Compose profile with database-specific Flyway migration; removed the personal Maven cache override and hardcoded JDK-path instructions. | `mvnw.cmd clean verify` passed with 40 tests, 0 failures, 0 errors, and 0 skipped; Compose syntax passed. Live Docker/PostgreSQL startup was intentionally deferred because Docker Desktop was not running. |
| 2026-08-12 | Item 8 crypto seams | Added application-owned payload-protection and hash-generation contracts, moved the local AES-GCM and SHA-256 implementations into `infrastructure.crypto`, and changed use cases to depend on the contracts. | `mvnw.cmd clean verify` passed with 41 tests, 0 failures, 0 errors, and 0 skipped. Encryption, hash, API, and stored-data behaviour are unchanged; a KMS-backed provider is not implemented. |
| 2026-08-12 | Item 9 API-key authentication | Added API-key authentication for operational APIs, payload-safe `401` handling, OpenAPI key documentation, and dev-profile-only Swagger/H2 exposure. | `mvnw.cmd clean verify` passed with 46 tests, 0 failures, 0 errors, and 0 skipped. The design uses one shared key; identity, roles, expiry, rotation, and secret-manager integration remain unimplemented. |
| 2026-08-12 | Item 10 repository cleanup | Removed the obsolete standalone schema and attestation placeholder, reconciled README instructions with the implemented service, and reformatted query-service validation and redaction grouping without changing behaviour. | `mvnw.cmd clean verify` passed with 46 tests, 0 failures, 0 errors, and 0 skipped. README claims only implemented behaviour; Docker/PostgreSQL runtime validation remains deferred. |
